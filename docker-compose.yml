services:
  web:
    build: .
    container_name: dinotour
    ports:
      - "5010:5010"
    environment:
      # Ambiente de produção
      - FLASK_APP=app.py
      - FLASK_ENV=production
    restart: unless-stopped
